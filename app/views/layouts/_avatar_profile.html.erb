<% if current_user.avatar.url.present? %>
  <%= image_tag current_user.avatar.url,
                class: 'rounded-circle',
                width: w, height: h %>
<% else %>
  <%= image_tag 'placeholders/placeholder.jpg',
                class: 'img-fluid rounded-circle',
                width: w, height: h %>
<% end %>